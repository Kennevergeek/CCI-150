Write code to reverse a C-style String. (Including the null terminator)

string cstring(string s)
{
  int len = s.length();
  for(int i=0; i < (len/2)+1; i++)
  { 
    int res = s[i];
    s[i] = s[len-i-1];
    s[len-i-1] = res;
  }
  return res;
}
